export const metadata = {
    title: 'About',
    description: 'next app',
}

const AboutPage = () => {
    return (
        <div>
            <h2>This is about page</h2>
        </div>
    );
};

export default AboutPage;