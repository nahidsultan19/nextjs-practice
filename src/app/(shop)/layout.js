import React from 'react';

const ShopLayout = ({ children }) => {
    return (
        <div>
            <h2>Shop Layout</h2>
            {children}
        </div>
    );
};

export default ShopLayout;