import React from 'react';

export const metadata = {
    title: 'Shop | Cart',
    description: 'next app',
}

const CartPage = () => {
    return (
        <div>
            <h2>Cart page</h2>
        </div>
    );
};

export default CartPage;